<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.onfit.mapper.ProductMapper">

    <select id="findAllProducts" resultType="ProductDto">
        SELECT * FROM products
        ORDER BY id DESC
    </select>

    <select id="findProductsByCategory" resultType="ProductDto">
        SELECT * FROM products
        WHERE category = #{category}
        ORDER BY id DESC
    </select>

    <insert id="insertProduct">
        INSERT INTO products (
            name, price, category, image_url, hover_image_url, recommended_seasons, style_tags
        ) VALUES (
                     #{name}, #{price}, #{category}, #{imageUrl}, #{hoverImageUrl}, #{recommendedSeasons}, #{styleTags}
                 )
    </insert>

</mapper>